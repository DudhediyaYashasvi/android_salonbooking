<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.snjb.myapplication">
    <!-- 🔐 Required for Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".ManageServicesActivity"
            android:exported="false" />
        <!-- 🔹 Launcher: Front Page -->
        <activity
            android:name=".FrontPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 🔹 Welcome Screen -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="false" /> <!-- 🔹 Authentication Screens -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".SignUpActivity"
            android:exported="false"
            tools:ignore="MissingClass" /> <!-- 🔹 Customer Flow -->
        <activity
            android:name=".LocationEntryActivity"
            android:exported="false" />
        <activity
            android:name=".SalonListActivity"
            android:exported="false" />
        <activity
            android:name=".ServiceBookingActivity"
            android:exported="false" />
        <activity
            android:name=".ViewBookingsActivity"
            android:exported="false" /> <!-- 🔹 Owner Flow -->
        <activity
            android:name=".OwnerLoginActivity"
            android:exported="false"
            tools:ignore="MissingClass" />
        <activity
            android:name=".OwnerDashboardActivity"
            android:exported="false"
            tools:ignore="MissingClass" />
        <activity
            android:name=".ManageSlot"
            android:exported="false" /> <!-- 🔹 Optional: Owner-User Chat -->

    </application>

</manifest>